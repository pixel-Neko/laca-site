<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verification Status</title>
    <style>
        :root {
            /* Minimalist Palette */
            --bg-body: #f1f5f9;
            --bg-card: #ffffff;
            --text-main: #1e293b;
            --text-sub: #64748b;
            --accent: #0f172a; /* Slate 900 */
            --accent-hover: #334155;
            
            --success-bg: #ecfdf5;
            --success-text: #059669;
            --error-bg: #fef2f2;
            --error-text: #dc2626;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-body);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .card {
            background: var(--bg-card);
            padding: 40px;
            border-radius: 12px; /* Slightly rounder */
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            text-align: center;
            max-width: 400px;
            width: 90%;
            border: 1px solid #e2e8f0;
        }

        .icon-container {
            margin-bottom: 20px;
        }

        .icon-circle {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
        }

        /* Dynamic Colors */
        .success-bg { background-color: var(--success-bg); }
        .error-bg { background-color: var(--error-bg); }
        
        .success-icon { color: var(--success-text); font-size: 40px; }
        .error-icon { color: var(--error-text); font-size: 40px; }

        h1 { margin: 0 0 10px 0; color: var(--text-main); font-size: 24px; font-weight: 700; }
        p { color: var(--text-sub); font-size: 16px; line-height: 1.5; margin-bottom: 25px; }

        .btn {
            display: inline-block;
            width: 100%;
            padding: 14px;
            background-color: var(--accent);
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            transition: background 0.2s;
            box-sizing: border-box;
        }

        .btn:hover { background-color: var(--accent-hover); }
    </style>
</head>
<body>

    <div class="card">
        <div class="icon-container">
            <div class="icon-circle <%= success ? 'success-bg' : 'error-bg' %>">
                
                <% if (success) { %>
                    <span class="success-icon">✓</span>
                <% } else { %>
                    <span class="error-icon">✕</span>
                <% } %>

            </div>
        </div>

        <h1><%= success ? 'Confirmed!' : 'Failed' %></h1>
        
        <p><%= message %></p>

        <a href="/" class="btn">Return to Home</a>
    </div>

</body>
</html>